<h1>目標一覧</h1>

<table>
  <thead>
    <tr>
      <th>タイトル</th>
      <th>作成日</th>
    </tr>
  </thead>
  <tbody>
    <% @goals.each do |goal| %>
      <tr>
        <td><%= goal.title %></td>
        <td><%= goal.created_at.strftime("%Y年%m月%d日") %></td>
      </tr>
    <% end %>
  </tbody>
</table>
